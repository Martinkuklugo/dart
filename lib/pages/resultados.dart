import 'package:flutter/material.dart';

class PaginaResultados extends StatelessWidget {
  const PaginaResultados({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text(
      "Resultados",
      style: TextStyle(fontSize: 25),),
    );
  }
}